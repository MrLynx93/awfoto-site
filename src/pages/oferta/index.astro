---
import { getCollection } from 'astro:content';
import BaseLayout from '../../layouts/BaseLayout.astro';
import OfferCard from '../../components/OfferCard.astro';
import ContactSection from '../../components/ContactSection.astro';
import { getSettings } from '../../lib/siteContent';

const settings = getSettings();
const offers = (await getCollection('offers')).sort((a, b) => a.data.order - b.data.order);
---

<BaseLayout
  title="Oferta"
  description="Rodzaje sesji: plenerowa, rodzinna, roczkowa i świąteczna."
>
  <header class="page-header">
    <p class="label">Oferta</p>
    <h1>Rodzaje sesji</h1>
    <p class="lead">
      Każda sesja wygląda inaczej, ale zawsze bez sztywnego pozowania.
      Ceny pakietów znajdziesz w <a href="/cennik">cenniku</a>.
    </p>
  </header>

  <section class="section">
    <div class="container">
      <div class="offers">
        {offers.map((offer) => <OfferCard offer={offer} />)}
      </div>
    </div>
  </section>

  <ContactSection
    settings={settings}
    heading="Nie wiesz, co wybrać?"
    lead="Napisz, opowiedz o okazji — podpowiem, która sesja pasuje najlepiej."
    warm
  />
</BaseLayout>

<style>
  .page-header {
    max-width: 46rem;
    margin: 0 auto;
    padding: clamp(3.5rem, 8vw, 6rem) var(--pad-x) 0;
    text-align: center;
  }
  .page-header h1 {
    margin-bottom: 1.25rem;
    font-size: clamp(2.8rem, 6.5vw, 4.5rem);
  }
  .page-header p {
    margin-inline: auto;
  }

  .offers {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: clamp(1.5rem, 3.5vw, 3rem);
  }

  @media (max-width: 820px) {
    .offers {
      grid-template-columns: 1fr;
      max-width: 24rem;
      margin-inline: auto;
    }
  }
</style>
